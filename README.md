# vc-common-service
A multi-tenant API and UI providing a format-agnostic abstraction layer for Verifiable Credential operations. It normalizes issue/verify/hold patterns across credential formats and back-end agents.
