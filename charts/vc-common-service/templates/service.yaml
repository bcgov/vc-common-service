apiVersion: v1
kind: Service
metadata:
  name: {{ include "vc-common-service.fullname" . }}
  labels:
    {{- include "vc-common-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vc-common-service.selectorLabels" . | nindent 4 }}
