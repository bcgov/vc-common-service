name: CI

on:
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci-checks:
    permissions:
      contents: read
      pull-requests: write
    uses: ./.github/workflows/ci-checks.yml
    secrets: inherit
